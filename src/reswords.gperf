%compare-strncmp

%{
#include <string.h> // Needed for strcmp
#include "parser.tab.h"
%}

struct resword { char *name; yytoken_kind_t type; };

%%
int, INT
char, CHAR
return, RETURN
if, IF
else, ELSE
do, DO
while, WHILE
const, CONST
%%